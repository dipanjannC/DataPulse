# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Data directories (keep samples only)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep

# Real synthetic-pipeline config (only .sample.json is tracked)
data/sample/synthetic_config.json

# Environment
.env
.env.local

# SQLite databases (regeneratable from CSVs)
*.db
!src/db/sales.db

# Derived data-quality report (regeneratable from CSVs + schema.json)
src/data/quality_report.json

# Eval harness outputs (regeneratable; live checkpoint is resume state)
eval_report.json
eval_checkpoint.jsonl

# Coverage
htmlcov/
.coverage
.coverage.*

.pytest_cache
.mypy_cache/
.ruff_cache/

# Logs
*.log

# Presentation deck + any generator (local only — do not push to remote)
docs/*.pptx
docs/_deck/

# Claude Code local config (do not commit)
.claude/
